/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.api-sports.io'],
    },
    reactStrictMode: true,
    swcMinify: true,
}

module.exports = nextConfig
